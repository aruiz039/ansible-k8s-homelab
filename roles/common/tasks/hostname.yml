---
- name: Set hostname from inventory
  ansible.builtin.hostname:
    name: '{{ inventory_hostname }}'
- name: Render /etc/hosts from template
  ansible.builtin.template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: '0644'
    backup: true
